{
  "name": "zkpassport-proof-verifier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node src/server.ts | npx pino-pretty",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "node --require ts-node/register --test src/**/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^11.3.0",
    "@zkpassport/sdk": "^0.17.0-beta.2",
    "@zkpassport/utils": "^0.38.0-beta.2",
    "fastify": "^5.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "pino-pretty": "^13.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
