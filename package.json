{
  "name": "zkpassport-proof-verifier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node src/server.ts | npx pino-pretty",
    "build": "tsc",
    "start": "node dist/server.js",
    "test": "node --require ts-node/register --test src/**/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@zkpassport/sdk": "0.14.0-beta.1",
    "@zkpassport/utils": "0.36.0-beta.1",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "pino-pretty": "^13.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
